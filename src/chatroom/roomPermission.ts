// import { getUsername, getUserWallet } from '@server/activitiesTracker/FTScrape';

// async function getRoomPermission(channel_id: string) {
//   const username = await getUsername(channel_id);
//   const wallet = await getUserWallet(channel_id);
// }
